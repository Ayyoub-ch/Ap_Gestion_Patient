<header class="header" data-turbo="false">
    <nav>
        <div class="logo">
            <a href="{{ path('app_main') }}">
                <img src="{{ asset('img/logo/logo.png') }}" class="logo-image" alt="Logo">
            </a>
        </div>
        <ul class="nav-list">
            {% if is_granted('ROLE_USER') %}
                <li><a href="{{ path('app_main') }}">Accueil</a></li>
            {% endif %}

            {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_INFIRMIER') %}
                <!-- Menu déroulant pour l'admin -->
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        Administrateur
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_admin_dashboard') }}"><i class="fas fa-tachometer-alt"></i> Tableau de bord</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_users') }}"><i class="fas fa-users"></i> Utilisateurs</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_services') }}"><i class="fas fa-hospital"></i> Services</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_chambres_lits') }}"><i class="fas fa-bed"></i> Chambres & Lits</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_patients') }}"><i class="fas fa-user-injured"></i> Patients</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_statistiques') }}"><i class="fas fa-chart-bar"></i> Statistiques</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_admin_parametres') }}"><i class="fas fa-cog"></i> Paramètres</a></li>
                    </ul>
                </li>
            {% endif %}

            {% if is_granted('ROLE_ADMINISTRATIF') %}
                <!-- Menu déroulant pour l'administratif -->
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        Gestion
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_administratif') }}">Tableau de bord</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_patient') }}">Patients</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_sejour') }}">Séjours</a></li>
                    </ul>
                </li>
            {% endif %}

            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <!-- Menu déroulant utilisateur -->
                <li class="dropdown user-dropdown">
                    <a href="#" class="user-profile dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        <span class="user-circle">
                            <span class="user-icon">👤</span>
                        </span>
                        <span class="user-name">{{ app.user.prenom }} {{ app.user.nom }}</span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="{{ path('app_profile') }}">Profil</a></li>
                        <li><hr class="dropdown-divider"></li>
                        <li><a class="dropdown-item" href="{{ path('app_logout') }}">Se déconnecter</a></li>
                    </ul>
                </li>
            {% else %}
                <li><a href="{{ path('app_login') }}">Se connecter</a></li>
            {% endif %}
        </ul>
    </nav>
</header>